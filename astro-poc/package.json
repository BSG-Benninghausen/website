{
  "name": "vereins-baukasten-astro",
  "type": "module",
  "private": true,
  "scripts": {
    "sync-assets": "mkdir -p public && rm -rf public/assets && cp -r ../assets public/assets",
    "gen-pwa": "node scripts/gen-pwa.mjs",
    "predev": "npm run sync-assets && npm run gen-pwa",
    "prebuild": "npm run sync-assets && npm run gen-pwa",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "devDependencies": {
    "astro": "^6.4.8"
  }
}
